# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage

# Node
node_modules/
dist/
.vite/
*.local

# Env — never commit real credentials
.env
.env.*
!.env.example

# Uploads / local artifacts
backend/uploads/
backend/.models/

# Database dumps. These contain resume text, email addresses and the whole
# application history — everything the RLS policies exist to protect. Never
# commit one, least of all to a public repository.
backups/
*.sql.gz
*.dump

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
